nav(class='navbar sticky-top navbar-expand-md navbar-dark bg-dark' mb-4)
  a(class='navbar-brand' href='/') Nalkutin
  button(class='navbar-toggler' type='button' data-toggle='collapse' data-target='#navbarNavAltMarkup' aria-controls='navbarNavAltMarkup' aria-expanded='false' aria-label='Toggle navigation')
    span(class='navbar-toggler-icon')
  div(class='collapse navbar-collapse' id='navbarNavAltMarkup')
    ul(class='navbar-nav')
      li.nav-item.active
        a(class='nav-link' href='/') Home
          span(class='sr-only')(current)
      li.nav-item
        a(class='nav-link' href='/tasks') Tasks
      li.nav-item
        if !loggedIn
          a(class='nav-link login' data-toggle='modal' href='#login') Log in
        else
          a(class='nav-link' href='/logout') Log out
